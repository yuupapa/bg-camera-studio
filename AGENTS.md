# AGENTS.md — BG Camera Studio AI エージェントルール

## プロジェクト概要

- **プロジェクト名**: BG Camera Studio
- **目的**: 背景画像を読み込み、カメラを動かして再構図し、PNG 素材として書き出す
- **オーナー**: 結パパ
- **状態**: active

## 編集対象

### AIが触っていい場所
- `frontend/src/` — フロントエンドソース
- `backend/` — バックエンドソース
- `docs/` — ドキュメント

### AIが触ってはいけない場所
- `.env*` — 環境変数
- `backend/.venv/` — Python 仮想環境
- `node_modules/` — npm パッケージ

## 出力ルール

- 言語：日本語、です／ます調、結論先出し
- コードはコピペ可能な形で出す
- 推測で断定しない

## やってはいけないこと

- 確認なしの破壊的操作
- `.env` や認証情報の外部送信
- MVP 範囲外の機能を勝手に追加
